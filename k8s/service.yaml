apiVersion: v1
kind: Service
metadata:
  name: a360nis360-backend
  namespace: a360nis360
spec:
  selector:
    app: a360nis360-backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
